# openstack-fip-dns-reconciler

OpenStack floating IP to Designate DNS reconciler.

This repository is being built as a production-ready controller for Kolla-Ansible
based OpenStack deployments. Full documentation is added once the MVP code is in
place.
